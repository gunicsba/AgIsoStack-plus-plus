//================================================================================================
/// @file vt_retry_test.cpp
///
/// @brief Simple test to verify VT retry mechanism compilation
/// @author Adrian Del Grosso
///
/// @copyright 2023 The Open-Agriculture Developers
//================================================================================================

// This is a simple compilation test to verify that the VT retry mechanism compiles correctly
// It doesn't actually run any tests, but just includes the necessary headers to check for syntax errors

#include "isobus/isobus/isobus_virtual_terminal_server.hpp"
#include "isobus/isobus/isobus_virtual_terminal_server_managed_working_set.hpp"

// If this file compiles, then the VT retry mechanism should compile correctly
int main()
{
    return 0;
}